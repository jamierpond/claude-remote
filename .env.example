# Access PIN — used for authenticating from your phone
CLAUDE_REMOTE_PIN=change-me-to-a-secure-pin

# Domain configuration (required for CORS and VAPID push notifications)
CLIENT_URL=https://your-client-domain.com
SERVER_URL=https://your-server-domain.com

# Optional: additional CORS origins (comma-separated)
# CORS_ORIGINS=https://other-origin.com
